apiVersion: v1
kind: Service
metadata:
  name: community-service
  namespace: default
spec:
  selector:
    app: community-service
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
